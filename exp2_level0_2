#include <stdio.h>
int main() 
{
    int a;
    scanf("%d",&a);
    if(a>=0)
    {
        printf("the given number is positive");
    }
    else if (a==0)
    {
        printf("the given is zero");
    }
    else
    {
        printf("the given number is negative");
    }
}

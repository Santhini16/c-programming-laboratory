#include <stdio.h>
int main() 
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(a==b)
    {
        printf("the two numbers are equal");
    }
    else
    {
        printf("the two numbers are not equal");
    }
}

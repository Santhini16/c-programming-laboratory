#include <stdio.h>
int main()
{
    int a,b,c,d;
    scanf("%d%d%d%d",&a,&b,&c,&d);
    if((a==c && b==d)||(a==b && c==d)||(a==d && b==c))
    {
        printf("rectangle");
    }
    else
    {
        printf("not rectangle");
    }
}

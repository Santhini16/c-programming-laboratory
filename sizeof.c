#include <stdio.h>
int main() 
{
  int a;
  float b;
  char c;
  double d;
  printf("the size of int:%d bytes\n",sizeof(a));
  printf("the size of float :%d bytes\n",sizeof(b));
  printf("the size of char:%d bytes\n",sizeof(c));
  printf("te size of double:%d bytes\n",sizeof(d));
}

#include <stdio.h>
int main() 
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a>b)
    {
        int max=a;
        printf("the maximum is %d",max);
            }
    else
    {
        int max=b;
        printf("the maximum is %d",max);
    }
}
